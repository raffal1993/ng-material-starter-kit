import { NgModule } from '@angular/core';
import { HashPipe } from './hash.pipe';

@NgModule({
  imports: [],
  declarations: [HashPipe],
  providers: [],
  exports: [HashPipe]
})
export class HashPipeModule {
}
